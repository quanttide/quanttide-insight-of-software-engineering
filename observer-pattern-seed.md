# 观察者雏形：三要素已齐，缺的是确认权可分配

承接 [programming-agent.md](programming-agent.md) 的“已有雏形”一句，展开现场看到的东西。

**预测**：观察者的专门设计，关键不在“再造一个观察者”——判断权分离、中间表示、分歧显性化这三件事在现场都已有实例；缺的是**确认权可分配**。若确认权继续集中在一人身上，观察者机制越精致，单点负担越重。

**一、判断权与写入权分离**：执行云的建模原则是「AI 建议 + 人确认」——**AI 不直接修改（判断不是事实）**。AI 有生成与建议权，判别落地与否的权力不在它手里。（`profile/qtcloud-execute/studio.md`、`profile/qtcloud-execute/index.md`）

**二、清点与判断分离**：设计评审工作流的第一步「清点」，要求把图上真有的东西看清楚——逐区写进报告的「看到」一节，**这一节只清点、不下判断，别写「混乱」「不合理」这类词**，也禁编造。观察与判断被拆成两道工序，各由不同步骤承担。（`profile/quanttide/workflows/design-review.yaml`）

**三、中间表示**：知识工作云的对话里，AI 出的流程是一张**流程草案卡**——步骤清单，每步标执行者（AI／机器／人）与判据条数，**人确认后才跑**。AI 的隐式模型被外化成了一张人可审的卡片。（`apps/qtcloud-work/src/studio/doc/views/chat.md`）

**四、分歧显性化**：设计评审的判断步骤要求——先把主诊断写成一句话，**与人的第一眼对照：一致就说明理由，不一致就说明为什么**；支撑证据至多五处、每处指到图上位置；其余观察移进「旁枝」当附录；图上读不出、要人确认的移进「拿不准」。分歧不是要被抹平的噪声，是被显式记录的对象。（同上）

**定位表述**：资产云的愿景把这套东西的名字点出来了——**把人类的认知负担从「创作」降维为「司法复核」**；AI 是流水线上严格工序里的熟练工，人退到复核位置。（`domains/quanttide-asset/data/context/default/vision.md`）

## 缺口：确认权是单点（2026-09-12 补充）

现有工作流的判断点多数写成同一句话——**创始人确认**：发布放行（`devops-release.yaml`）、抽取值不值得（`code-implement-toolkit.yaml`）、studio 是否能不再依赖命令行（`code-implement-studio.yaml`）。每条流程插一个创始人确认判据，与 08-27 的自述对上：**很多关键且非常精密的调整，还是需要创始人自己来动**。

**但已经有例外**：`code-implement.yaml` 里人的判断点写成「文档定稿（内容取舍只能人拍）」与「代码验收（能不能上平台）」——**没绑定到具体个人**。说明确认权可分配的形式已经在编程流里出现了，缺的是把它从例外变成常规。

## 可测形式

数两个东西：**创始人确认点的数量与确认耗时**，同时看把关强度（返工次数、错漏）有没有下降。

- 确认点/耗时下降且把关强度不降 → 确认权确实可分配，观察者成立
- 确认点更多更细、耗时上升 → 只是把负担结构化了，没减少——设计失败

## 边界（2026-09-12 修正：可以切）

原先“编程的工序最难切细、切口不明”的说法**不成立——可以切**，切法在知识工作手册里已定型：手册按三件组织（[产物规范](https://github.com/quanttide/quanttide-handbook-of-knowledge-work/blob/main/piece/artifact.md) / [任务规范](https://github.com/quanttide/quanttide-handbook-of-knowledge-work/blob/main/process/task.md) / [工作区](https://github.com/quanttide/quanttide-handbook-of-knowledge-work/blob/main/place/workspace.md)）——**任务规范**定“任务是产物之间的流动关系，按「目标/步骤/验收」三段编写”，**工作区规范**定一次工作的边界如何划。

落到编程，`code-implement` 工作流就是切好的样子：

- **切口是用例**：outline（逐动作、逐字段、逐判据列清单，来源是已实现的东西——不漏、也不凭空多）→ doc（重写三部分文档，真实业务写成用例沉在使用指南里，做过几件写几件）→ test（照用例设计测试，每条注释写出处；写对账脚本做**双向对账**，文档用例号与测试出处两边集合必须相等）→ code（写实现让测试变绿）
- **防虚假对齐**：实现缺席时测试必须是红的（挡恒绿空壳）
- **文档先行、代码最后**：要改从文档改起
- **人的判断点被压到两处**：文档定稿、代码验收；其余四步都是 AI 执行

**由此修正的推论**：编程不是难切，而是**切口的选取与知识工作不同**——知识工作的切口是产物类别，编程的切口是**用例**。而用例同时就是观察者需要的那个中间表示：人可审查、可对账、可干预。所以编程域的观察者雏形，已有比一条建模原则更完整的形态——**用例 + 双向对账 + 红绿约束**。

